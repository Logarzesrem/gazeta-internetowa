{% extends 'base.html.twig' %}

{% block title %}{{ 'article.new.title'|trans }}{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="row">
            <div class="col-md-8">
                <h1>{{ 'article.new.title'|trans }}</h1>

                {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <button type="submit" class="btn btn-primary">{{ 'article.create'|trans }}</button>
                    <a href="{{ path('app_article_index') }}" class="btn btn-secondary">{{ 'article.back_to_list'|trans }}</a>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %} 